server:
  port: 2001

spring:
  application:
    name: config
  cloud:
    config:
      username: lowen
      password: lowen
      profile: test
      label: master

#      直接指定配置服务器
      uri: http://localhost:2000/

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1000/eureka/


